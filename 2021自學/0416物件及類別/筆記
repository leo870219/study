物件中的數值可以是字串、數字、陣列、函式等等，而字串、數字、陣列都是物件的屬性，函式則為物件的方法

可透過點記法 (Dot notation) 存取物件的屬性與函式。
物件名稱作為命名空間 (Namespace) —為了能存取物件所封裝的所有東西
接著你寫一個「點」以及你所想存取的項目，可能是簡單屬性的名稱、陣列屬性的項目，又或是針對物件函式之一的呼叫。

括弧記法 (Bracket notation) 是另個存取物件屬性的方法。
例如:
person['age']
person['name']['first']

括弧記法不僅可動態設定數值，亦可設定名稱。

物件中的this指向該物件

其實每次我們透過內建的瀏覽器 API 或 JavaScript 物件寫出範例時，我們就是在用物件

例如:
myString.split(',');
就是在使用 String 類別實例可用的方法。每次只要你在程式碼中建立字串，該字串就會自動建立成為 String 的實例，並具備有多個常見的方法與屬性。

根據類別建立物件實體 (Object instance)，即該物件包含了類別中所定義的資料與功能。
在根據類別建立物件實體時，就是執行類別的「建構子 (Constructor) 函式」所建立。
而這個「根據類別來建立物件實體」的過程即稱為「實體化 (Instantiation)」。